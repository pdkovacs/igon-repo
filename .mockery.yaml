quiet: False
disable-version-string: True
with-expecter: True
mockname: "{{.InterfaceName}}"
filename: "{{.MockName}}.go"
outpkg: mocks
packages:
  iconrepo/internal/app/services:
    config:
      dir: "test/mocks"
    interfaces:
      Repository:
        