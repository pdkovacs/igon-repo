apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: iconrepo-backend-ingress
  namespace: default
spec:
  virtualhost:
    fqdn: iconrepo-backend.local.com
  routes:
    - conditions:
      - prefix: /
      services:
        - name: iconrepo-backend
          port: 8080
