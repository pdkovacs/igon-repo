html {
  box-sizing: border-box;
}
*, *:before, *:after {
  box-sizing: inherit;
}

.iconrepo-app {
	.app-message-list {
		width: calc(100% - 100px)
    position: absolute;
		left: 50px;
	}
	.main-screen {
		min-height: 100%;
		min-width: 100%;
		position: absolute;
		top: 0;
		left: 0;
		z-index: -1;
	}
}
