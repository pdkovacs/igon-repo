package config

// Version holds the version of the application
var Version = "development"

// Commit is the commit id of the input to the build
var Commit = "sha1"

// BuildUser is who built the app
var BuildUser = "build.user"

// BuildTime is when the app was built
var BuildTime = "build.time"
